# Services line style study — September 5, 2026

Active design experiment requested by Bridget: extend the Services icons' champagne line art and edges across the site, beginning with the homepage demo unit. This is a component comparison, not a second active website. Its rounded bevel direction was subsequently approved for root rollout.

Open `index.html` through the repository's local server. Compare Outline console (recommended), Studio window, Line-art rack, and the current demo frame at desktop and phone widths. Each direction also shows matching photo and inquiry samples. The inquiry sample does not submit. The actual existing demo playlist and visualizer scripts power the audio unit.

`preview.html` includes the existing homepage demo component and Services SVG references, with root CSS and assets loaded through a base URL. `preview.css` holds experimental overrides; no root website CSS, media choices, or player code is changed. The comparison's Current unit option restores the unit's frame, not the study's surrounding layout.

Approved for rollout: Bridget requested committing and pushing the study, then applying its beveling throughout the root website. Use the recommended Outline console as the demo frame and the shared rounded bevel treatment for large panels. Preserve the homepage's selected media, real content, audio behavior, and the separate pending Community title-finish decision.

Bridget’s follow-up adds shallow bevels to all three proposed frames, title bars, faders, navigation buttons, photo frames, and inquiry controls. Highlights sit on the top/left edges, with small lower shadows and recessed fields. The original frame comparison stays intact.

The next refinement follows Bridget’s rounded-corner reference: large outer frames use 16–18px desktop corners and 14px phone corners. Inset screens and photo edges have matching smaller curves; compact controls retain their existing corners.

Root rollout implemented in `checkmark-rounded-bevels.css`. This comparison remains available at its existing review URL; it preserves the alternatives and the original unit comparison.
